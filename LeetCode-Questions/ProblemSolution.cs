namespace LeetCode_Questions
{
    class ProblemSolution
    {
        internal bool ValidateSolutionAgainst(object key, object value)
        {
            return ProplemFunction(key).Equals(value);
        }


        private object ProplemFunction(object input)
        {
            return null;
        }
    }
}
